{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main desktop window",
  "windows": ["main", "preview"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "positioner:default"
  ]
}
