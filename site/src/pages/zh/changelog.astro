---
import SiteFooter from "../../components/SiteFooter.astro";
import SiteLayout from "../../layouts/SiteLayout.astro";
---

<SiteLayout
  lang="zh-CN"
  path="/zh/changelog/"
  title="mclip 更新日志"
  description="mclip 的公开版本更新记录。"
>
  <main class="text-page">
    <a class="back-link" href="/zh/">返回首页</a>
    <h1>更新日志</h1>
    <article class="release-note">
      <p class="eyebrow">0.2.0 · 开发中</p>
      <h2>敏感内容展示保护与来源应用排除</h2>
      <p>
        新增本地、有界、版本化的高置信度敏感文本检测。桌面历史与 CLI/Agent 的默认输出会遮罩已分类内容；用户仍可通过明确的界面操作或单次 <code>--raw</code>/<code>--reveal-secrets</code> 参数查看原文，复制动作继续使用原始内容但不在结果中回显。
      </p>
      <p>
        Privacy Preferences 可按稳定应用标识排除捕获，并明确展示平台能力。macOS 使用 bundle ID、Windows 使用可执行文件名、X11 使用 <code>WM_CLASS</code>；纯 Wayland 当前不可用，不会被描述为已执行排除。
      </p>
      <p>
        遮罩是展示保护，不是静态加密。历史仍以本地明文保存；启发式检测和来源识别可能误报或漏报，不能替代密码管理器、DLP 或泄露防护。
      </p>
    </article>
    <article class="release-note">
      <p class="eyebrow">0.2.0 · 开发中</p>
      <h2>文本快速操作与可组合 CLI 管道</h2>
      <p>
        文本详情新增 JSON 格式化/压缩、Base64 和 URL 组件编解码。结果先在独立窗口预览；复制走普通剪贴板流程，替换原记录必须再次确认并保留稳定 ID 与置顶状态。
      </p>
      <p>
        <code>mclip-cli transform</code> 使用内容唯一的 stdout，且不读取历史或写剪贴板；<code>copy --stdin</code> 与隐式管道只把唯一 UTF-8 输入写入系统剪贴板，不直接修改历史。输入限制为 1 MiB，输出限制为 4 MiB。
      </p>
    </article>
    <article class="release-note">
      <p class="eyebrow">0.1.1</p>
      <h2>更快、更清晰的完整核心体验</h2>
      <p>
        完成 Tailwind CSS 4 界面迁移和紧凑密度优化，新增跟随系统/浅色/深色主题、最多 500 条历史、可配置主界面与历史分组条数、序号和主窗口 Logo。文本与文件行更紧凑，图片行保持完整缩略图高度；主窗口、分组列表和详情窗口继续独立。
      </p>
      <p>
        新增可最大化/恢复并支持 Escape 的独立图片查看器；搜索、键盘和鼠标使用同一个激活目标。颜色代码、Emoji、长文件名与完整文件路径展示也得到完善。
      </p>
      <p>
        运行时现在只让主窗口进入启动关键路径，preview 预热，About、Preferences 和图片查看器按需创建；历史使用带 revision 的 snapshot/delta 更新，图片读取使用有界单飞缓存。Apple M2 实测托盘就绪中位数提升 51.3%，重复打开图片查看器中位数从 384.62 ms 降到 49.37 ms。
      </p>
      <p>
        Agent CLI 页识别未安装、旧版、未知、当前和较新版本，提供安装、升级或重装且不自动降级；桌面端和公开安装脚本都会验证同一 Release 的 SHA-256 资产后再替换旧 CLI。
      </p>
    </article>
    <article class="release-note">
      <p class="eyebrow">0.1.0</p>
      <h2>首个公开版本</h2>
      <p>
        支持 macOS 和 Windows 的托盘剪贴板历史、文本/图片/文件保存与回填、搜索、独立预览窗口、偏好设置和中英文界面。
      </p>
    </article>
  </main>
  <SiteFooter lang="zh" />
</SiteLayout>
