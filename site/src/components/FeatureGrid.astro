---
interface Feature {
  title: string;
  body: string;
}

interface Props {
  id: string;
  eyebrow: string;
  title: string;
  features: Feature[];
}

const { id, eyebrow, title, features } = Astro.props;
---

<section class="section" id={id}>
  <div class="section-heading">
    <p class="eyebrow">{eyebrow}</p>
    <h2>{title}</h2>
  </div>
  <div class="feature-grid">
    {
      features.map((feature) => (
        <article class="feature-card">
          <h3>{feature.title}</h3>
          <p>{feature.body}</p>
        </article>
      ))
    }
  </div>
</section>
